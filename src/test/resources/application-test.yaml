api:
  guardian:
    apiKey: test-key
    baseUrl: guardian-base-url/
  one:
    baseUrl: one-base-url/
  webClient:
    base:
        connectTimeoutMs: 200
        connectionRequestTimeoutMs: 100
        readTimeoutMs: 1000
        poolSize: 1500
        totalRequestTimeout: 1200
    override:
        connectTimeoutMs: 100
        connectionRequestTimeoutMs: 50
        readTimeoutMs: 500
        poolSize: 750
        totalRequestTimeout: 800
spring:
  config:
    activate:
      on-profile: test
